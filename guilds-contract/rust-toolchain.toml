[toolchain]
# Pinned (Approach A — see docs/delegate-stability.md). A
# stable-channel bump rotates contract WASM bytes; a fresh
# `code_hash` mints a NEW `guilds-contract` instance, orphaning
# every existing guild membership state.
channel = "1.93.0"
targets = ["wasm32-unknown-unknown"]
components = ["rustfmt", "clippy"]
